#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define mod 1000000007
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    stack<int> st;
    st.push(1);
    st.push(2);
    st.push(3);
    st.pop();
    cout<<st.top()<<endl;
    queue<int> q1;
    q1.push(1);q1.push(2);q1.push(3);
    q1.pop();
    while(!q1.empty()){
        cout<<
    }
    return 0;
}
