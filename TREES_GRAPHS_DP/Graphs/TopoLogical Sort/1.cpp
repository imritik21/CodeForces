#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define mod 1000000007
void solve()
{
}
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    
    vector<vector<int>> mat = {{1,4}.{2,3}};
    map<set<int>,int> mp;
    int n = mat.size();
    for(int i=0;i<n;i++){
        for(auto& it:mat[i]){
            mp[];// how should i insert col ele in map set
        }
    }
    return 0;
}
